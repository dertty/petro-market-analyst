# Состояние рантайма: в живой установке 262 МБ, из них 256 МБ в state/cx.
# Без этой строки каждая пересборка гонит их в демон Docker.
data/

# История git не нужна ни одному из образов; в vendor/ouroboros/.git её особенно много.
.git
vendor/ouroboros/.git
