data/
.env

# Векторный индекс — генерат: пересобирается из reports/pdf одной командой
# (scripts/build_index.py) и весит мегабайты, которым нечего делать в истории.
reports/index/

# Байткод Python — генерат
__pycache__/
*.pyc

# Кэши инструментов и артефакты прогонов — не часть проекта
.firecrawl/
.playwright-mcp/
.pytest_cache/
eval_runs/
